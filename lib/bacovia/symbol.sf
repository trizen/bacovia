#!/usr/bin/ruby

class Symbol(name) {

    method ==(Symbol o) {
        name == o.name
    }

    method ==(_) {
        false
    }

    method pretty {
        "#{name}"
    }

    method to_s {
        "Symbol('#{name}')"
    }
}
